项目代码参考正点原子库函数版本:
MCU: STMF03ZET6
资源：
	64KB SRAM,
	512KB FLASH, 
	定时器
		2个基本定时器、
		4个通用定时器、TIM2，TIM3， TIM4， TIM5
		2个高级定时器、TIM1，TIM8
	2个DMA控制器（共12个通道）
	3个SPI、2个IIC
	5个串口
	1个USB
	1个CAN
	3个12位ADC、1个122位DAC
	1个SDIO接口
	1个FSMC接口
	112个通用IO口


 
项目达到程度：
需求1：尽量利用所有资源，把例程都跑一遍

需求2：有bootloader, 可以通过串口进行升级  
1、调试串口 
    -> 添加串口，复习串口配置以及串口调试助手等使用，配置串口中断。已实现
    -> 串口配置DMA，串口接收数据，串口发送数据，配置串口空闲中断 已实现
2、模拟IIC读写（参考IIC实验）

3、调试SPI（参考SPI实验）

需求3：
通过串口进行IAP升级：
1、bootloader:

2、APP代码更新：
	将接收到的完整的固件，按照指定的位置写入flash中，写入完成之后，会在flash预留的标志位处，写入一个完成的标志。
	代码更新完成后，就会进行软复位，从头再次开始判断是否需要升级（判断依据就是完成的标志位）
	更新代码，将下载区接收的完整代码读取出来，烧录到APP代码区实现更新。
